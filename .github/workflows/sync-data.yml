name: Sniper Pharma Inteligente
on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'
    - cron: '0 5 1,16 * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    steps:
    # 1️⃣ CHECKOUT PRIMEIRO
    - uses: actions/checkout@v4
    
    # 2️⃣ PYTHON
    - uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    # 3️⃣ *** INSTALAR ANTES ***
    - name: 📦 INSTALL PACKAGES
      run: |
        pip install requests pandas openpyxl
    
    # 4️⃣ AGORA PYTHON FUNCIONA
    - name: 🎯 CAPTURA
      run: python app.py
    
    - name: 🧹 LIMPEZA
      run: python limpeza.py
    
    # resto igual...
