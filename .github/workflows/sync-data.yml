name: Sniper Inteligente
on:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with: { python-version: '3.10' }
      - name: Install Dependencies
        run: pip install requests pandas
      
      - name: 🎯 Sniper (Captura)
        id: sniper
        run: python app.py
        
      - name: 🧹 Faxineiro (Limpeza)
        run: python limpeza.py
        
      - name: 💾 Salvar Dados
        run: |
          git config --global user.name 'Sniper Bot'
          git add .
          git commit -m "🤖 Dados Atualizados" || echo "Sem mudanças"
          git push
          
      - name: 🔄 Efeito Dominó
        if: steps.sniper.outputs.trigger_next == 'true'
        run: gh workflow run main.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
